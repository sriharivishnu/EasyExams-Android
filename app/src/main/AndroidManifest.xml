<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.corp.srihari.deca">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/round_pre_good"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/round_pre_good"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExamActivity" />
        <activity android:name=".Learn" />
        <activity android:name=".EndGame" />
        <activity android:name=".WrongAnswersActivity" />
        <activity android:name=".FullExam" />
        <activity android:name=".PerformanceIndicators" />
        <activity android:name=".ChooseExam"></activity>
    </application>

</manifest>